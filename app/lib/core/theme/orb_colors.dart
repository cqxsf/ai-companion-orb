import 'package:flutter/material.dart';

class OrbColors {
  OrbColors._();

  // Background
  static const Color bgBase = Color(0xFF0A0A0F);
  static const Color bgCard = Color(0xFF12121A);
  static const Color bgElevated = Color(0xFF1A1A25);
  static const Color bgOverlay = Color(0xFF252530);

  // Borders
  static const Color borderSubtle = Color(0x14FFFFFF);
  static const Color borderActive = Color(0x26FFFFFF);

  // Text
  static const Color textPrimary = Color(0xF2FFFFFF);
  static const Color textSecondary = Color(0x99FFFFFF);
  static const Color textTertiary = Color(0x59FFFFFF);
  static const Color textMuted = Color(0xFF94A3B8);

  // Emotion: Calm (purple)
  static const Color calm = Color(0xFF6366F1);
  static const Color calmLight = Color(0xFF818CF8);
  static const Color calmGlow = Color(0x4D6366F1);

  // Emotion: Joy (amber)
  static const Color joy = Color(0xFFF59E0B);
  static const Color joyLight = Color(0xFFFBBF24);
  static const Color joyGlow = Color(0x4DF59E0B);

  // Emotion: Care (pink)
  static const Color care = Color(0xFFEC4899);
  static const Color careLight = Color(0xFFF472B6);
  static const Color careGlow = Color(0x4DEC4899);

  // Emotion: Listen (green)
  static const Color listen = Color(0xFF10B981);
  static const Color listenLight = Color(0xFF34D399);
  static const Color listenGlow = Color(0x4D10B981);

  // Emotion: Concern (blue)
  static const Color concern = Color(0xFF3B82F6);
  static const Color concernLight = Color(0xFF60A5FA);
  static const Color concernGlow = Color(0x4D3B82F6);

  // Emotion: Excited (red)
  static const Color excited = Color(0xFFEF4444);
  static const Color excitedLight = Color(0xFFF87171);
  static const Color excitedGlow = Color(0x4DEF4444);

  // Emotion: Sleep (near-black)
  static const Color sleep = Color(0xFF1E293B);
  static const Color sleepLight = Color(0xFF334155);
  static const Color sleepGlow = Color(0x1A1E293B);
}
